<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imsjkumar.myworktodo" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_todo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_todo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyWorkToDo" >
        <activity
            android:name=".AddEdtNoteActivity"
            android:theme="@style/MyTheme.MyWorkToDo"
            android:screenOrientation="portrait"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>